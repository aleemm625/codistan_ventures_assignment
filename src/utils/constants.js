const bookingStatus = {
  CONFIRMED: 'confirmed',
  PENDING: 'pending',
  CANCELLED: 'cancelled',
  OVERED: 'overed'
};

const paymentStatus = {
  PAID: 'paid',
  UNPAID: 'unpaid'
};

const roomType = {
  SINGLE: 'single',
  DOUBLE: 'double'
};

module.exports = {
  bookingStatus,
  paymentStatus,
  roomType
};
